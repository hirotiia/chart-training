import { ContentLayout } from '@/components/layouts/content-layout';
import { Button } from '@/components/ui/button';

const LoginPage = () => {
  return (
    <ContentLayout title="ログイン">
      <Button>クリック！</Button>
    </ContentLayout>
  );
};

export default LoginPage;
